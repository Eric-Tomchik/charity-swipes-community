# Public Convex deployment URL for the production build.
# Not a secret (it is visible in the client bundle), committed so Cloudflare builds
# cannot silently produce a bundle with no backend if the dashboard variable is missing.
VITE_CONVEX_URL=https://trustworthy-octopus-88.convex.cloud
